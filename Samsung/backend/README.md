# Snovia Backend (Simulation Layer)

## Setup

1. Copy `.env.example` to `.env` and set `MONGODB_URI`.
2. Install deps:
   - `npm install`

## Seed products (10–12 predefined)

- `npm run seed`

## Run API

- `npm run dev`

### Endpoints

- `GET /api/products/:productId`
- `POST /api/warranties`

