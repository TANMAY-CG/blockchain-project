# Sealed Frontend

This folder now contains starter single-page interfaces:

- `sealed-problems.html` -> auto-captured build/runtime issue dashboard
- `index.html` -> OTP verification + warranty viewer starter UI

Use backend APIs from `http://localhost:4601` and problems server at `http://localhost:4600`.

